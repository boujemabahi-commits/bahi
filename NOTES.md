# Developer Notes — Architecture Decision & Phase 2 Path

> **Status (Phase 10, 2026-09-17) — public center signup with platform-admin approval.** The first piece of new product surface after the migration: a prospective center fills `/register-center` (public, `guest` middleware, `App\Livewire\Public\CenterSignup` on the login shell — `layouts/guest.blade.php` now takes a `$title`/`$maxWidth`) and only a `CenterSignupRequest` row is written (password hashed at submission; email must be unique against `users` AND non-rejected requests, so a rejected applicant can resubmit); no Tenant, User or session exists until a platform admin approves. **Platform admins** are users with `is_platform_admin = true` and `tenant_id = NULL` (`users.tenant_id` was already nullable since Phase 2 — verified with `PRAGMA table_info`, so no altering migration), created only by `php artisan make:platform-admin {email} {name} {password}` (`App\Console\Commands\MakePlatformAdmin`; no roles, nothing can approve the first admin). They live in `/admin` (`App\Livewire\Admin\SignupRequests` on a minimal `layouts/admin.blade.php`; pending/approved/rejected tabs with counts, approve/reject with a reason). **Approval** runs in one transaction: `Tenant` (slug = `Str::slug($name, '-', 'ar')` — Arabic transliterates — with `-2`, `-3` suffixes on collision, `'center'` fallback), owner `User` (tenant_id stamped, the already-hashed password copied as-is — the `hashed` cast leaves an existing hash untouched via `Hash::isHashed`), `Permissions::OWNER_ROLE` assigned, request marked with reviewer + time + tenant. No demo seeders run: a real center starts empty (verified: 0 teachers/courses/students, 1 user). **Two-way separation, not just permissions:** the tenant route group is now `['auth', 'tenant-user']` (`EnsureTenantUser` redirects a platform admin to `/admin` and 403s any tenant-less non-admin), `/admin` is `['auth', 'platform-admin']` (`EnsurePlatformAdmin`: tenant users of any role get 403), both aliases in `bootstrap/app.php` and both added to Livewire's persistent middleware, and the login page sends `is_platform_admin` users to `/admin` instead of `/dashboard`. `TenantScope` additionally adds `1 = 0` for a signed-in user with no tenant, so even a mis-routed admin reads zero tenant rows (verified). That change surfaced a real bug in the Phase 9 recursion guard: the guard's own session lookup is a `User` query, and with a tenant-less user the outer lookup got the `1 = 0` clause while the inner one didn't, so `Auth::user()` flip-flopped and login failed. The scope now skips the `User` model until `Auth::hasUser()` is true (i.e. the guard's session/remember-token/credentials lookups are never tenant-filtered; every later User query is), and the static re-entrancy flag is gone. Verified end to end in the browser: admin → `/admin`, blocked from `/dashboard`/`/students`; public signup → pending → approve → new owner logs in to an empty center and adds staff via Phase 9's Team panel; second request rejected with a visible reason and no rows created; duplicate pending email and existing-user email both rejected with a clear message; tenant owner/reception/accountant get 403 on `/admin`. Nothing committed.
>
> **Status (Phase 9, 2026-09-17) — roles & permissions real and enforced end-to-end; core feature list closed.** Each center's owner manages staff accounts alone, with no platform-level involvement after the center exists. **Accounts:** `users.status` (نشط/متوقف) added; `User` now uses `BelongsToTenant`, so every user query is tenant-scoped like every other business model (`TenantScope` gained a re-entrancy guard because resolving the authenticated user is itself a `User` query). A paused account is refused at login with an explicit Arabic message (`LoginForm::authenticate`) and any open session it still has is signed out on its next request (`EnsureUserIsActive`, appended to the `web` group). **Permissions:** the catalog and the three built-in roles live in `App\Support\Permissions` and are seeded by `RolePermissionSeeder` (idempotent, `syncPermissions` each run): مدير المركز = everything; إدارية / استقبال = manage-students, manage-enrollments, manage-payments, manage-attendance; محاسب = manage-expenses, manage-salaries, view-reports. `TenantSeeder` was already assigning the owner role (confirmed, unchanged); `StaffUserSeeder` adds `reception@{slug}.test` and `accountant@{slug}.test` per tenant (password `password`). **Enforcement:** `routes/web.php` wraps every module in Spatie's `permission:` middleware (aliases registered in `bootstrap/app.php`; Spatie's middleware is also added to Livewire's persistent list so `/livewire/update` calls re-check it). Dashboard, Notifications and Settings stay open to all staff; the sidebar, dashboard quick actions and the Settings tab strip hide anything the user can't open, and a denied request renders a proper Arabic `errors/403` page in the app shell. The header/sidebar/account role label is now the real role. **Panels:** `Settings\Team` (list, create with an owner-set initial password, edit incl. optional new password, نشط/متوقف toggle — no delete; the role select never offers مدير المركز, and `tenant_id` is stamped from the creating owner, never from input) and `Settings\Roles` (three built-ins read-only with their permission chips + per-tenant custom roles: create/edit/delete-when-unused with a permission checklist). Both `abort(403)` in `mount()` and in every action for anyone without `manage-users`. **Custom-role scoping decision:** Spatie's `teams` mode was not enabled (it changes three pivot tables, needs a per-request team id and adds per-team role assignment we don't need). Instead `roles` gained `tenant_id` + `display_name`, `App\Models\Role` (set as Spatie's role model) stores a custom role under the namespaced unique name `tenant-{id}:{label}` and exposes `label`, and `Role::forTenant()/assignable()` are the only ways the panels and the role select read roles — verified: two tenants can each own a "مسؤول التسويق" with different permission sets, and neither can list, assign or edit the other's. **Decisions to flag:** Schedule and Teachers/Courses/Groups stayed owner-only as specified (a center wanting reception to edit the schedule can grant it through a custom role); Settings' **معلومات المركز** tab is visible to everyone but editable only with `manage-settings` (form is `inert` + a note otherwise), since letting reception rename the center seemed wrong even though the tab is "open"; `view-reports` covers Statistics as well as Reports, so the accountant also sees student-growth trends there; notifications remain tenant-wide for all staff (routing them by role/permission is a small follow-up now that roles exist — `Notification.user_id` already supports per-user targeting). **This closes the core feature list from the original Phase 1 brief.** Anything beyond is new product surface, not migration work — e.g. a self-serve center signup/provisioning flow for onboarding new customers (distinct from staff management inside an already-provisioned center), password reset by email, real PDF export, salary period history. Nothing committed.
>
> **Status (Phase 8, 2026-09-17) — migration complete.** Dashboard, Reports and Statistics are real and `app/Support/Mock` is deleted (nothing references it; `composer dump-autoload` run). All three pages are plain controllers (`DashboardController`, `ReportsController`, `StatisticsController`) rendering the Phase 1 Blade views, fed by one shared, tenant-scoped aggregate class `App\Support\Analytics`: stat cards (trends only for total students and this month's enrollments, where last month is a fair baseline — the rest are `null`), revenue vs. expenses and student growth per month (bucketed in PHP, so no DB-specific date SQL), enrollments per month, collection rate (`Σ(price − discount)` vs. `Σ remaining`), course/teacher leaderboards, latest enrollments, today's remaining classes (via `ScheduleSlot::parseTimeRange()`), retention (نشط share), average enrollment value, attendance rate / absences / lateness this month, and salary totals (refreshing balances the same way the Salaries page does). The Expenses category tiles moved into `Analytics::expensesThisMonth()` so the Expenses page and the Reports card share one implementation. Month labels use Carbon's `ar_MA` locale (شتنبر, not سبتمبر) to match `ar_date()`. Statistics' period select is wired to `?months=6|3|0` (0 = since January) and the trend cards compare the first and last month of the chosen window. The dashboard greeting uses the signed-in user's name. Two side fixes: the login redirect no longer uses `navigate: true` (the SPA transition skipped `DOMContentLoaded`, so the dashboard charts stayed blank right after login), and `StudentSeeder` clamps `registered_at` to today so no seeded enrollment is dated in the future. **Deferred by decision:** team/multi-user + roles management (Settings tabs المستخدمون / الصلاحيات / الإشعارات / اللغة / المظهر remain placeholders; notifications stay tenant-wide until then), real PDF export on Reports, and salary period history (one running balance per teacher, no period column). Nothing committed — everything since the Phase 1 baseline is uncommitted for review.
>
> **Status (Phase 7, 2026-09-17):** Notifications are real and event-driven. `App\Models\Notification` (custom table, not Laravel's Notifiable) with `notify()` and a `visibleTo(user)` scope — `user_id` NULL is tenant-wide, set is per-user. Enrollments (create), Payments, Expenses (create) and Salaries (pay) call `Notification::notify()` and dispatch `notification-created`; `App\Livewire\Notifications\Bell` (embedded in the header, replacing the hardcoded array and static "4 جديدة") listens for it and also polls every 30s, showing a real unread count and per-item mark-read; `Notifications\Index` has الكل / غير المقروءة, per-item and mark-all read, paginated. `NotificationSeeder` ports the Phase 1 feed with real timestamps. Settings' **الحساب** tab is real (`Settings\Account`: name, unique email, password change gated by Laravel's `current_password` rule); the "المنصب" / phone / avatar fields were dropped since the users table has no such columns. Center Profile was already real. Still mock/placeholder: Dashboard, Reports, Statistics, and Settings' المستخدمون / الصلاحيات / الإشعارات / اللغة / المظهر tabs.
>
> **Status (Phase 6, 2026-09-17):** Payments, Expenses and Salaries are real. **Payments** (`App\Models\Payment`, `App\Livewire\Payments\Index`): recording a payment calls `Enrollment::applyPayment()`, which lowers the student's current enrollment balance, derives مكتمل/جزئي/غير مؤدي, syncs the student's badge via the shared `syncStudent()`, and advances the due date when an overdue period is settled in full (so the monthly rollover doesn't flip it straight back). The form only lists students who still owe something and caps the amount at their balance; receipts are still a placeholder. **Expenses** (`App\Models\Expense`, CATEGORIES constant): full CRUD, category tiles are real current-month sums (tile click = filter), avg-daily divides by the day of month. **Salaries** (`App\Models\SalaryPayment`): one running balance per teacher, recomputed on every load from the salary structure — fixed teachers get `fixed_salary`, commission teachers get `commission_rate` % of this month's payments from their students; `paid` is only changed by "تسجيل دفعة". No period column exists, so this is not monthly payroll history. **Schedule fix:** `save()` now rejects room and teacher double-bookings using real time-interval overlap (edges may touch), `ReferenceDataSeeder` was changed so seeded groups never collide, and the grid flags any remaining conflicts with a red "تعارض" badge. Still on Phase 1 mock data: Dashboard, Notifications, Reports, Statistics, Settings.
>
> **Status (Phase 5, 2026-09-17):** Attendance and Schedule are now real. `App\Livewire\Attendance\Index` keeps a `states` array (student_id → حاضر/متأخر/غائب) edited in memory via `wire:click` and persisted by `save()` as one `AttendanceRecord` per (student_id, date) — the new unique index on that pair makes re-saving an update; `group_id` is intentionally not part of the key since a student is in one group at a time. `App\Livewire\Schedule\Index` renders `ScheduleSlot` rows grouped by `ScheduleSlot::DAYS` into the same weekly grid, with create/edit/delete (group narrowed by course, teacher/room defaulted from the group). `ScheduleSlotSeeder` derives each group's slots from its `schedule` text; `AttendanceSeeder` writes the last 8 sessions per group on those weekdays. The student profile's الحضور tab reads real records; the old `$mockId` mapping is gone. `App\Support\Mock\Attendance` / `Mock\Schedule` remain only because the Dashboard still uses them. Still on Phase 1 mock data: Dashboard, Payments, Expenses, Salaries, Reports, Statistics, Notifications, Settings.
>
> **Status (Phase 4, 2026-09-17):** Enrollments is now real — `App\Models\Enrollment` + `App\Livewire\Enrollments\Index`, same pattern as the other real modules. Enrollment is the source of truth for the financial side of a registration: the form takes price / discount / paid and derives `remaining` + `status` (مكتمل / جزئي / غير مؤدي); saving an enrollment syncs the student's `course_id`, `group_id` and `financial_status` (مكتمل→مؤدي, جزئي→جزئي, غير مؤدي→غير مؤدي) but never touches `enrollment_status`. The student profile's التسجيلات tab and payments summary read the student's latest real enrollment. `EnrollmentSeeder` creates one enrollment per seeded student. Still on Phase 1 mock data: Dashboard, Attendance, Schedule, Payments, Expenses, Salaries, Reports, Statistics, Notifications, Settings (plus the header notifications / global search, and the Attendance/Payments-list/Activity tabs on the student profile).
>
> **Status (Phase 3, 2026-09-16):** Teachers, Courses and Groups are now real, following the Students pattern exactly — `App\Livewire\{Teachers,Courses,Groups}\Index` + `resources/views/livewire/*/index.blade.php`, tenant-scoped models with `$fillable`/relations/scopes, DB-backed search/filters/pagination, validated add/edit modals, soft delete with confirmation. Counts (`groups_count`, `students_count`) come from `withCount()` at query time; the seeded denormalized counter columns are no longer read. Teachers also gained a salary structure (`salary_type` ثابت/بالعمولة with `fixed_salary` / `commission_rate`) — stored only, the calculation belongs to the future Salaries module. Still on Phase 1 mock data: Dashboard, Enrollments, Attendance, Schedule, Payments, Expenses, Salaries, Reports, Statistics, Notifications, Settings (and the header notifications / global search).
>
> **Status (Phase 2, 2026-09-16):** the migration described at the bottom of this file has been carried out. The app is now a real Laravel 12 + Livewire 3 install; `App\Routing\Route`, `App\View\BladeLite` and `App\View\View` are gone, the Blade views were lifted over essentially unchanged, and the Students module runs on a real database with tenant scoping. See `SETUP.md` for the current state. Everything below is preserved as the Phase 1 history and rationale.

## Why Phase 1 wasn't a real Laravel install

The original brief called for real Laravel (Blade + Livewire + Tailwind). During setup, `composer create-project laravel/laravel` and every subsequent `composer install`/`composer require` attempt failed: this sandbox's organization-level network policy blocks `packagist.org`, `repo.packagist.org`, and `getcomposer.org`. A GitHub-mirror workaround was attempted and rejected. You (the user) were offered options and explicitly chose:

> "Plain PHP/Blade mockup, drop Livewire" — skip Composer entirely, hand-build a minimal PHP router + Blade-like templating shim, use Alpine.js instead of Livewire for interactivity.

Everything in this repo follows from that decision.

## What was built instead

A small, dependency-free PHP micro-framework designed to *look and feel* like Laravel conventions so migrating to real Laravel later is mechanical, not a rewrite:

- **`App\Routing\Route`** — static `Route::get($uri, $action)->name($name)` API, `{param}` placeholders, closures or `[Class, 'method']` callables. Mirrors Laravel's `Route` facade closely enough that `routes/web.php` reads like a real Laravel routes file.
- **`App\View\BladeLite`** — a from-scratch compiler that supports the actual Blade syntax used throughout this app: `@extends`, `@section`/`@endsection`, `@yield`, `@include`, `@if`/`@elseif`/`@else`/`@endif`, `@foreach`/`@endforeach`, `@forelse`/`@empty`/`@endforelse`, `@php`/`@endphp`, `{{ }}` (escaped) and `{!! !!}` (raw) echoes, and anonymous component tags (`<x-stat-card :value="$x" />`) with isolated variable scope and named slots. Compiled templates are cached to `storage/framework/views/` and only recompiled when the source `.blade.php` file's mtime changes — the same caching model Laravel uses.
- **Alpine.js** in place of Livewire for all client-side state: dropdowns, modals, tabs, the mobile drawer, the attendance status-cycling buttons, and the notifications filter all use `x-data`/`x-on`/`x-show`/`x-model` rather than server round-trips.
- **Fully vendored, zero-CDN assets**: Tailwind CSS (compiled via the Tailwind CLI, not the Play CDN), Alpine.js, Chart.js, lucide icons (as static SVG partials), and the Cairo/IBM Plex Mono fonts are all copied from `node_modules` into `public/build/`, `public/fonts/`, and `public/icons/` by `scripts/sync-vendor-assets.mjs`. The running app makes zero network calls — this matters both for this sandbox and for demoing the app anywhere without internet access.

## What's genuinely out of scope (by design, per the original brief)

Per the brief's explicit "do not implement" list for Phase 1, none of the following exist anywhere in this codebase, even though some page UIs (forms, "add" buttons, dropdown actions) visually suggest them:

- Real authentication / sessions / password handling
- A database, migrations, or any Eloquent-style models
- Real CRUD — every "Add", "Edit", "Delete" action shows a toast and changes nothing
- Multi-tenancy / tenant isolation
- Roles & permissions enforcement
- Any HTTP API
- Payment processing integration

All data on every page comes from `app/Support/Mock/*.php`, generated deterministically (index-based cycling, not `rand()`/`time()`), so the UI is stable and reviewable across reloads.

## Migrating to real Laravel + Livewire (Phase 2)

When Composer access is available (i.e., outside this sandbox, on your own machine or CI):

1. `composer create-project laravel/laravel planzeen-v2` and `composer require livewire/livewire`.
2. Move `resources/views/**` almost as-is — the Blade syntax used here (`@extends`, `@section`, `@foreach`, `{{ }}`, component tags) is standard Blade and will parse unmodified in real Laravel. The only things to double check: anonymous component slot edge cases and any place a `@php` block relies on this repo's specific helper functions.
3. Re-implement `app/helpers.php` functions (`mad()`, `ar_date()`, `initials()`, `is_active_route()`) as either global helpers (`app/helpers.php` autoloaded via `composer.json` `"files"`) or a Blade directive/service — they're plain PHP with no framework coupling, so this is a copy-paste.
4. Replace `App\Support\Mock\*` classes with real Eloquent models + migrations (Student, Teacher, Course, Group, Enrollment, AttendanceRecord, ScheduleSlot, Payment, Expense, SalaryPayment, Notification) — the mock classes' method names and return shapes (arrays of associative arrays) were deliberately kept close to what a `Model::all()->toArray()`/resource-collection call would return, to make this swap mostly mechanical.
5. Convert the highest-interactivity pieces (attendance status-cycling, notifications filtering, the search/filter bars, modals that will need real submit handlers) to Livewire components — Alpine's `x-data` state maps fairly directly onto Livewire component public properties, and Alpine can still be layered on top of Livewire (Livewire ships with Alpine already) for purely presentational interactivity like dropdowns/tabs.
6. Add real auth (Laravel Breeze/Fortify or Jetstream), multi-tenancy (a package like `stancl/tenancy` or a simple `team_id` scoping approach), and policies/permissions (Laravel's built-in `Gate`/`Policy` or Spatie's permission package) — none of this exists yet by design.
7. Re-point `routes/web.php` at real controllers instead of closures once controllers exist.
8. `npm run build` already produces the exact same compiled Tailwind CSS / vendored JS / icons / fonts setup real Laravel expects in `public/build` — no changes needed there beyond wiring up Vite (or keeping the Tailwind CLI build if preferred) in the new project's `package.json`.

## Known minor items

- `public/favicon.ico` was an empty placeholder file in the original Laravel skeleton; it's been replaced with a minimal generated icon (or removed — see repo state) so the browser tab doesn't request a 0-byte file.
- `storage/framework/views/*.php` (compiled Blade-lite cache) is regenerated automatically on first request; it's fine to delete the directory contents at any time.
- This is a sandbox-only workaround, not a recommendation — for any real Phase 2 work, use real Laravel + Livewire as originally specified. This repo exists solely so the Phase 1 UI could be built and verified end-to-end without Composer/network access.
